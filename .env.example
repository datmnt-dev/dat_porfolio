# Base URL of the separate portfolio-mail-api service.
# Vite injects this at build time, so configure it on your frontend host too.
# Local: http://localhost:5000
# Production example: https://portfolio-mail-api-ajbi.onrender.com
VITE_API_BASE_URL=http://localhost:5000
